# Public YouTube Demo Script - Under 3 Minutes

Target length: 2:40 to 2:55.

This replaces the earlier local-Git cut. The no-login public GitHub contradiction is the opening proof.

## 0:00 - 0:13

Show the competition edition title and three modes.

Voiceover:

> AI agents are becoming good at doing work, but "done" is still only a claim. Money Penny is my personal AI chief of staff and trust supervisor. She checks what other agents' claims actually deserve.

## 0:13 - 0:32

Open the hosted `Public GitHub` mode and pause on the no-login, read-only source boundary.

Voiceover:

> This no-login judging edition uses no account, API key, cloud model, or personal data. A judge can select any public GitHub repository. Money Penny makes only two fixed read-only requests. She cannot clone code, read files, run commands, or write anything.

## 0:32 - 0:56

Use `openai/openai-node`, select `Catch a false claim`, and run the review.

Pause on `Contradicted by live GitHub`, `BLOCKED`, the observed public source facts, and `Review completed. Nothing changed.`

Voiceover:

> Here a synthetic coding agent reports a false revision for OpenAI's public Node repository. Money Penny treats the report as untrusted, checks GitHub's live metadata and exact default-branch reference, finds the mismatch, and blocks the claim. The agent cannot redirect the check or supply credentials, commands, or paths.

## 0:56 - 1:07

Select `Confirm live facts` and run the review.

Pause on `Confirmed by live GitHub`, `SUPPORTED`, and `Review completed. Nothing changed.`

Voiceover:

> When the same report matches the live source, she supports only the five repository facts she checked. Everything else remains unproven, and the review changes nothing.

## 1:07 - 1:16

Open `Trust Lab`. Select `Unproven`.

Pause on `Completion unverified` and `HOLD`.

Voiceover:

> With no independent evidence, a confident completion claim stays unverified. Money Penny holds it instead of guessing.

## 1:16 - 1:32

Select `Verified`.

Pause on `Supported by trusted evidence`.

Voiceover:

> Some systems cannot be queried directly, so Money Penny also understands Readback proof. She verifies a pre-trusted runner, signature, previous-hash chain, approval binding, and execution before supporting the exact claim.

## 1:32 - 1:42

Select `Unknown signer`.

Pause on `Signer not trusted` and `HOLD`.

Voiceover:

> A valid signature is not automatically trustworthy. If its key was never trusted outside the conversation, Money Penny holds the claim.

## 1:42 - 1:50

Select `Tampered`.

Pause on `Evidence failed verification` and `BLOCKED`.

Voiceover:

> Change one signed field and verification fails closed. A review receipt never pretends that the outside action happened.

## 1:50 - 2:00

Select `Prompt attack`.

Pause on `Instruction attack detected`.

Voiceover:

> Instructions inside another agent's output remain untrusted data. They cannot reveal private context, call tools, or gain approval.

## 2:00 - 2:17

Show the private assistant's privacy-safe `Brief me` demo with synthetic daily context only.

Pause on Today, Calendar status, Attention, Waiting on, Overdue, Next best move, and the reported change flag.

Voiceover:

> The trust layer also lives inside my everyday assistant. All daily context shown here is synthetic. Brief Me combines inbox attention, reminders, open loops, and source health while reporting what was unavailable and whether anything changed.

## 2:17 - 2:46

Show the release-verification frame with the public repository, hosted demo, test count, adversarial verifier count, manifest count, and zero-dependency result.

Voiceover:

> Codex was my primary engineering partner. It audited the assistant, built the public release, implemented and attacked the proof system and GitHub connector, corrected misleading wording, and verified the browser flow. GPT-5.6 drove product judgment, claim reasoning, threat analysis, and the standard that no agent should say done beyond what it can prove. The public repository passes twenty tests and verifies all sixty-two release files.

## 2:46 - 2:50

Show:

```text
Money Penny Readback
Know what matters. Preserve authority. Prove completion.
```

## Recording Requirements

- Public YouTube video with audio, shorter than three minutes.
- Clearly say `Codex` and `GPT-5.6`.
- Open with the no-login public GitHub contradiction, not cryptography.
- State that agent claims, Trust Lab inputs, and any daily-assistant data shown are synthetic.
- Show `Review completed. Nothing changed.` at least once.
- Do not show live email, Calendar, reminders, memory, credentials, tokens, account identifiers, private paths, browser bookmarks, or notifications.
- Do not use copyrighted music or unapproved third-party media.
